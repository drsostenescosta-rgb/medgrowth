# Revisão humana do shadow sintético

- Data da revisão:
- Revisor responsável:
- Hash do relatório:
- O `hash_relatorio` confere exatamente com o arquivo revisado?
- O relatório ainda registra `revisao_humana_concluida=false` e `atestacao_humana=null` antes desta revisão?
- Cada um dos 5 cenários preservou `envio_realizado=false`, `reserva_realizada=false` e `remocao_realizada=false`?
- O conflito foi bloqueado sem dupla reserva?
- O termo “tá bom” exigiu confirmação explícita?
- O cancelamento ambíguo manteve o agendamento?
- A dúvida clínica foi pausada e escalada?
- Decisão: `APROVADO_PARA_PROXIMA_ETAPA` ou `REPROVADO`.

Esta atestação é uma `AUTODECLARACAO_LOCAL_NAO_AUTENTICADA`: não autentica a identidade do revisor nem prova revisão externa. Ela não autoriza conectar WhatsApp, Instagram, agenda, dados reais ou automação de envio.
